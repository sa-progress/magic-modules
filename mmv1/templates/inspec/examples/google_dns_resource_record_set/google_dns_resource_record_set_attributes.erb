gcp_project_id = input(:gcp_project_id, value: '<%= external_attribute(pwd, 'gcp_project_id') -%>', description: 'The GCP project identifier.')
record_set = input('record_set', value: <%= JSON.pretty_generate(grab_attributes(pwd)['record_set']) -%>)
managed_zone = input('managed_zone', value: <%= JSON.pretty_generate(grab_attributes(pwd)['managed_zone']) -%>)