gcp_project_id = input(:gcp_project_id, value: '<%= external_attribute(pwd, 'gcp_project_id') -%>', description: 'The GCP project identifier.')
license_code = input('license_code', value: <%= JSON.pretty_generate(grab_attributes(pwd)['license_code']) -%>, description: 'License codes are mirrored across all projects that have permissions to read the License Code.')