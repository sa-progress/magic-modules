gcp_project_id = input(:gcp_project_id, value: '<%= external_attribute(pwd, 'gcp_project_id') -%>', description: 'The GCP project identifier.')
compute_image = input('compute_image', value: <%= JSON.pretty_generate(grab_attributes(pwd)['compute_image']) -%>, description: 'Compute image description')